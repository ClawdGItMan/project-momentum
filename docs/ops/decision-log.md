# Decision Log

| Date | Decision | Reason | Owner | Revisit Trigger |
| --- | --- | --- | --- | --- |
| 2026-03-14 | Use `Project Momentum` as the temporary codename. | The workspace needs a usable identity before final brand naming is complete. | Max | Revisit once brand testing produces a stronger final name. |
| 2026-03-14 | Run a docs-first founder OS before starting app code. | Clear context, research discipline, and scope control matter more than premature implementation. | Max | Revisit if the docs stop accelerating decisions. |
| 2026-03-14 | Target a two-founder, iPhone-first prototype sprint. | The fastest path to conviction is a focused mobile experience rather than a broad platform strategy. | Max | Revisit if target users strongly prefer another platform or distribution path. |
| 2026-03-14 | Lead with social accountability for self-improvement, using health and fitness as the first wedge. | This aligns with the clearest user behavior and keeps the product emotionally concrete. | Max | Revisit after interviews if another wedge shows stronger demand. |
| 2026-03-14 | Exclude finance from v0.1 positioning and scope. | Finance comparison risks making users feel worse and distracts from the healthier initial identity. | Max | Revisit only if strong interview demand emerges later. |
| 2026-03-14 | Use Expo / React Native as the default prototype direction. | It balances speed, polish, and future viability for an iPhone-first demo. | Max | Revisit if prototype needs clearly exceed Expo’s speed advantage. |
| 2026-03-14 | Update living docs after every meaningful session. | The workspace should act as durable project memory, not a stale archive. | Max | Revisit if the process becomes too heavy or redundant. |
| 2026-03-16 | Expand the v0.1 social model from squads-only to selective sharing across friends and squads. | The product should support trusted selective sharing beyond one small-group format while still avoiding public-social drift. | Max | Revisit if user testing shows the audience model is confusing or too broad for early adoption. |
| 2026-03-16 | Use friend invites and mutual acceptance instead of a follower model in v0.1. | Friends are simpler and more intuitive for the first release, while followers can be explored in a later version if needed. | Max | Revisit once v0.1 usage shows whether a broader social graph is needed. |
| 2026-03-16 | Aim for a premium-fitness visual direction with restrained color and a few meaningful animations. | The app should feel high-quality and desirable without drifting into luxury branding or noisy gamified UI. | Max | Revisit if early design explorations feel too sterile, too flat, or too intense. |
| 2026-03-16 | Increase the target squad size range to 3–15 people. | Larger squads can still feel intimate while fitting more realistic friend-group and community use cases. | Max | Revisit if feed quality or reciprocity breaks down in larger groups. |
| 2026-03-16 | Make the integration architecture adapter-friendly from day one, with Apple Health prioritized and Strava next in line. | The product should be easy to extend across health data sources without forcing multiple costly live integrations in the first sprint. | Max | Revisit if the sprint proves a second live provider is essential for conviction. |
| 2026-03-16 | Upgrade the repo to a build-first multi-agent framework with explicit design, frontend, backend/integrations, and QA roles. | The previous framework was strong on research and planning but underpowered for actually shipping a high-quality app. | Max | Revisit if the role system becomes too heavy or redundant for the team. |
| 2026-03-16 | Make workouts and habits the default friend-visible categories in v0.1. | These are the safest, clearest social signals to share by default while other categories remain selectively shared. | Max | Revisit if early users want a different privacy baseline. |
| 2026-03-16 | Use consistency as a first-class metric tied to check-ins and habit completion, including workouts. | The product needs a clear signal that someone is actively showing up, not just logging isolated actions. | Max | Revisit once the exact calculation and visualization have been tested in the prototype. |
| 2026-03-16 | Treat Apple Health as a required v0.1 integration, with manual entry only as fallback. | Real health connectivity is core to the credibility of the first product experience. | Max | Revisit only if a hard technical blocker threatens the sprint. |
| 2026-03-16 | Use an Expo custom iOS development-build workflow with prebuild instead of optimizing around Expo Go. | Apple Health requires native configuration and HealthKit capability setup, so a development build is the fastest credible path. | Max | Revisit only if the native setup meaningfully slows iteration more than it helps prototype credibility. |
| 2026-03-16 | Define consistency v0 as a 7-day score plus label with 40% check-ins, 40% habits, and 20% workouts. | The prototype needed an implementation-ready model that could appear across onboarding, profile, habits, and feed without reopening scoring debates. | Max | Revisit after user testing shows whether the score feels motivating or overly clinical. |
| 2026-03-16 | Make the first build slice onboarding -> Apple Health -> skippable squad step -> first workout check-in -> seeded Home. | This is the smallest slice that proves identity, selective sharing, real-data posture, and social payoff in one session. | Max | Revisit if demos show that another destination communicates the product better than Home. |
| 2026-03-16 | Keep squad selection as a suggested, skippable onboarding step. | Squads need to stay central without creating enough onboarding friction to block the first successful check-in. | Max | Revisit if early users skip squads so often that the accountability model weakens. |
| 2026-03-16 | Persist non-sensitive demo session state across reloads, but do not persist raw Apple Health snapshots. | The prototype needed repeatable demos without turning local storage into a broader health-data cache. | Max | Revisit if live demos require offline metric persistence or stronger encrypted local storage. |
| 2026-03-16 | Narrow HealthKit consent to the read-only MVP bundle and remove unused Health write messaging. | The current v0.1 product only needs workout, steps, sleep, and active-energy reads, so broader write consent would overstate the privacy surface. | Max | Revisit when the product adds a real HealthKit write path. |
| 2026-03-16 | Add dev-only demo controls plus a dedicated demo runbook and EAS profiles. | The team needs a repeatable way to stage connected, preview, fallback, and reset flows across browser, Xcode, and EAS contexts without changing the core product. | Max | Revisit if the controls start leaking into the real product experience or demo needs stabilize. |
| 2026-03-16 | Disable the Expo dev-client network inspector by default in iOS pod properties. | The demo/dev-client build did not need extra runtime inspection surface turned on by default. | Max | Revisit if local debugging friction increases enough to justify re-enabling it explicitly. |
| 2026-03-17 | Enable `ios.buildReactNativeFromSource` in `ios/Podfile.properties.json`. | React Native prebuilt artifacts failed CocoaPods validation under the current repo path with spaces, while source builds installed cleanly. | Max | Revisit if the repo moves to a no-space path or upstream prebuilt handling improves. |
| 2026-03-17 | Patch the EXConstants CocoaPods phase and the app target bundle phase to quote shell-resolved paths in Xcode. | The repo path contains a space, and both Expo Constants and the React Native bundle phase broke on unquoted script paths during simulator builds. | Max | Revisit if the repo path changes or an upstream Expo / React Native fix removes the need for local shell-script patching. |
| 2026-03-17 | Share the real `ios/` project in git, but keep Pods, build artifacts, and local tool folders ignored. | Apple Health and the Expo development-build workflow depend on native iOS project files, so collaborators need the checked-in Xcode project rather than a docs-only or prebuild-only handoff. | Max | Revisit if the team changes native workflow or decides to regenerate iOS entirely from config on each machine. |
| 2026-03-17 | Use a private GitHub repo as the active collaboration backbone for the two-founder build loop. | The project has moved from local-only prototyping into real shared implementation, so code, docs, and native iOS changes need one canonical remote. | Max | Revisit if the team moves to a different hosting model or splits product/docs from app code. |
| 2026-03-17 | Use Supabase for auth, Postgres, row-level security, and optional Realtime, with a single TypeScript Hono API and worker service for privileged backend work. | This gives the sprint the fastest path to real accounts, privacy enforcement, and relational product data without fragmenting the system into too many services before the product core is proven. | Max | Revisit if Supabase auth, RLS, or operational constraints slow the sprint more than they accelerate it. |
| 2026-03-17 | Keep Apple Health device-first, but move Strava and WHOOP behind a server-managed OAuth and webhook boundary. | Apple Health is an on-device HealthKit integration, while Strava and WHOOP are true remote providers that require token custody, refresh handling, rate-limit awareness, and webhook verification. | Max | Revisit if product scope changes or if a provider proves too high-friction for the sprint. |
| 2026-03-17 | Prioritize the backend phases as trust model first, private social core second, integrations third, and advanced personalization after that. | The app already demos well locally, but the main missing layer is real privacy and durable social data rather than algorithmic sophistication. | Max | Revisit if user testing shows integrations matter more urgently than real squads, habits, and feed persistence. |
| 2026-03-17 | Use Supabase email/password plus a single onboarding bootstrap path as the first real account flow. | This is the fastest credible way to get real accounts and profile creation in place without waiting on Sign in with Apple or multiplying auth states before the private social core is stable. | Max | Revisit when the backend-connected prototype is stable enough to justify adding Sign in with Apple. |
| 2026-03-17 | Keep `useMomentumSession()` as the stable client-facing facade while the app migrates from seeded state to backend-backed repositories. | The mobile app already has a working UX layer, so preserving one facade reduces screen churn and lowers the risk of breaking the demo while auth, profile, squad, habit, and feed data sources are swapped underneath it. | Max | Revisit after the app is fully backend-backed and the data layer deserves a cleaner split into dedicated hooks or stores. |
| 2026-03-17 | Centralize onboarding, squad creation, and invite acceptance in database RPCs plus security-invoker read views. | The Phase 0 backend needs atomic validation, RLS-safe read models, and a low-friction way to evolve the mobile app from seeded state without scattering trust logic across the client. | Max | Revisit if the RPC-heavy shape starts slowing iteration or if the service layer needs to own more orchestration. |
| 2026-03-17 | Use the hosted Supabase project `madwefunqkqppamlgzno` as the active remote backend for Project Momentum Phase 0. | The repo now needs one real remote database and auth project so the team can move from local-only scaffolding into actual accounts, schema validation, and backend-connected app work. | Max | Revisit only if the project is recreated, moved to another org, or replaced for operational reasons. |
| 2026-03-17 | Use a backend-only `backend/.env` plus a local-only `sslmode=no-verify` Supabase pooler connection string for hosted Phase 0 development. | The hosted Supabase project is reachable and the backend boots locally with this setup, while stricter pooler certificate verification currently fails from this machine. This keeps local progress moving without exposing server secrets to the mobile client. | Max | Revisit before broader team rollout, production deployment, or if a verified SSL connection path becomes available. |
| 2026-03-18 | Use GitHub feature branches, pull requests, and the shared `verify` workflow as the default two-founder collaboration workflow, with a local pre-push `main` guard until paid branch protection is available. | The repo now has real app, native iOS, docs, Supabase, and backend surfaces in motion at the same time, so parallel work needs a clear merge bar and a safer shared workspace than direct pushes to `main`. The current private GitHub plan blocked server-side branch protection, so local enforcement needed a fallback. | Max | Revisit if the GitHub plan changes, the CI suite needs to expand, or a different branching model proves faster without raising regression risk. |
| 2026-03-18 | Simplify founder collaboration into an AI-first GitHub issue -> pull request -> merge workflow, with Max as the default Build Owner and Merge Owner. | The previous collaboration instructions were too technical for two non-technical founders. The simpler model keeps GitHub as the shared home, lets AI handle most implementation and doc upkeep, and limits real build responsibility to one machine so the product stays safe without making both founders act like developers. | Max | Revisit if the team becomes more hands-on technically, if a second person needs full native build access, or if the workflow stops feeling simple enough to use consistently. |
| 2026-03-18 | Make Phase 1 mobile integration use hosted Supabase views/RPCs directly for founder-alpha flows, and include live squad chat as a required supporting surface. | The app needed to stop depending on seeded local state or a localhost-only bootstrap path so two real founders can sign in, onboard, publish persisted check-ins, and coordinate inside private squads. Hosted Supabase already owns the trust boundary for these operations, and squad chat materially strengthens squads without changing the feed-first thesis. | Max | Revisit if privileged orchestration grows enough that more mobile mutations must move behind the Hono service, or if chat starts distracting from the proof/feed loop. |
| 2026-03-18 | Keep founder-alpha social expansion explicit and low-friction: exact-username friend invites, in-app squad creation, and raw token acceptance for both friend and squad invites. | This is enough to let two real users form the private graph and validate squads/chat/check-ins without building public search, deep-link plumbing, or broader invite management before the thesis is proven. | Max | Revisit when founder-alpha usage shows whether token paste is too clunky or pending-invite surfaces become necessary. |
| 2026-03-18 | Show the generated raw invite token immediately inside the squads surface after sending a friend invite, creating a squad, or generating a squad invite token. | Token acceptance is the current founder-alpha redemption path, so the sender needs an in-app way to hand off the token without waiting for pending-invite inboxes or polished deep-link sharing flows. | Max | Revisit once deep links, pending invite surfaces, or broader invite orchestration become worth the added complexity. |
| 2026-03-18 | Make owned squads the primary squad-invite surface, with direct friend-targeted squad invite tokens plus one open-token fallback. | This matches the private, trust-based product thesis better than leaving squad invites as a generic action on every membership row, and it makes the intended founder-alpha behavior clearer: create your own squad, then bring your friends in deliberately. | Max | Revisit if squad admins beyond the owner need first-class invite powers, or if broader alpha testing shows that pending invite inboxes or deep links matter more than direct friend targeting. |
| 2026-03-18 | Handle iPhone keyboard avoidance in the shared `ScrollScreen` primitive instead of patching each text-entry screen separately. | Account creation and adjacent flows all depend on the same scroll container, so centralizing keyboard insets keeps fields reachable during typing without introducing inconsistent per-screen layout behavior. | Max | Revisit if any screen needs a custom keyboard choreography that the shared scroll behavior cannot support cleanly. |
| 2026-03-18 | Treat the onboarding squad step as truly optional in runtime state by clearing stale local squad ids before onboarding completion and friends-visible check-in publish. | Founder-alpha users can legitimately finish onboarding without a squad, and older seeded/persisted draft state should never block entry into the app or attach an invalid squad to a friends post. | Max | Revisit if onboarding begins requiring a live squad join or if invite-driven pre-onboarding squad selection becomes a first-class flow. |
| 2026-03-18 | Expose client-facing Supabase RPCs from `public` as thin wrappers over `app_private` trust functions. | Supabase mobile `rpc(...)` calls resolve against `public`, while the actual validation and mutation logic should remain in `app_private`. Public wrapper functions keep the client contract stable without moving sensitive logic out of the private schema. | Max | Revisit if the app moves to schema-qualified RPC calls across every client, or if the backend fully owns these mutations instead of direct mobile RPC access. |
| 2026-03-19 | Keep the `squad_memberships` visibility policy helper-based instead of re-reading `public.squad_memberships` inside its own `using` clause. | Founder-alpha setup surfaced a live `42P17` recursion failure because the policy was self-querying the same relation during evaluation. Delegating the membership check to the existing `security definer` helper preserves the intended access rule while removing the recursion trap. | Max | Revisit if squad visibility rules expand beyond active-member checks or if the helper layer is replaced with denormalized membership views. |
| 2026-03-19 | Add `app_private.can_view_squad_membership(...)` and rebuild the `squads`, `squad_memberships`, and `squad_invites` select policies around helper calls. | The hosted founder-alpha retest still needed a durable, checked-in fix after the live recursion error. Centralizing squad visibility into definer helpers removes recursive policy paths, keeps the RLS contract readable, and lets bootstrap/chat reads succeed without widening access. | Max | Revisit if squad visibility expands to pending members, role-based exceptions, or denormalized membership caches. |
| 2026-03-19 | Give existing authenticated users a dedicated account/settings hub instead of replaying onboarding. | The product needed one clear place for Apple Health state, sign-out, and future destructive-account actions without confusing the onboarding flow for new users. | Max | Revisit if the team decides to merge settings into Profile again or if onboarding replay becomes a first-class requirement. |
| 2026-03-19 | Implement account deletion through a service-role `DELETE /me` route plus a `transfer_squad_ownership` RPC guard. | Founder-alpha needs a safe way to let users permanently delete accounts without orphaning active squads. Blocking deletion until ownership is transferred, or allowing it when no owned squads remain, preserves the private squad model while keeping the backend behavior predictable. | Max | Revisit if the product introduces a different squad ownership model or a softer account-deletion policy. |
| 2026-03-19 | Promote squad management to a top-level `Squads` tab while keeping `/connections` as a hidden legacy redirect. | Squad selection, chat entry, invites, and recovery paths now matter enough to deserve a first-class destination, but older fallbacks and links should keep landing cleanly during the IA transition. | Max | Revisit if friend-graph setup or health recovery grows into its own dedicated top-level destination. |
| 2026-03-18 | Treat direct `NativeModules.AppleHealthKit` fallback plus native error surfacing as the primary Apple Health debugging posture for founder-alpha device testing. | The `react-native-health` package wrapper proved brittle on the current React Native architecture, and generic simulator-era messaging was hiding the real causes of on-device failures. Preserving native authorization errors and declaring the HealthKit system capability gives the team a more honest path to diagnosing signing, permissions, and coverage issues on a real iPhone. | Max | Revisit if upstream `react-native-health` export behavior stabilizes across the current RN architecture or if a different HealthKit bridge replaces it. |
| 2026-03-18 | Treat `Apple Health connected locally, but backend summary save failed` as a distinct runtime state from true HealthKit permission or bridge failures. | Founder-alpha debugging showed the app was collapsing Supabase RPC failures into a generic Apple Health error, which made a backend schema-exposure bug look like a device permission problem. Keeping the local connection state intact while surfacing the backend save error makes retries more honest and protects the product from misleading sync states. | Max | Revisit if a fuller connection-state model or background retry system replaces the current inline persistence flow. |
| 2026-03-18 | Make `app_private.touch_updated_at()` row-shape-safe instead of assuming every triggered row exposes `updated_at` as a direct field. | Direct founder-alpha verification revealed that the consistency recompute path could hit trigger-backed updates where `new.updated_at` was not safely addressable, which broke onboarding after the Apple Health step. Updating the trigger to patch `updated_at` through a row-shape-safe record transformation keeps the shared trigger reusable without forcing every caller to debug trigger internals. | Max | Revisit if the schema standardizes enough that a stricter typed trigger becomes preferable again. |
| 2026-03-19 | Add a shared starter squad called `Day ones` that users can join during onboarding. | The squad step needs one clear, low-friction option so new users can immediately enter a real accountability room instead of facing an empty choice or leaving onboarding without a social anchor. | Max | Revisit if the starter squad becomes too noisy, the privacy model changes, or curated cohort-specific starter squads replace one default room. |
| 2026-03-19 | Gate authenticated routing on an explicit bootstrap status and clear user-scoped frontend state on auth-user changes. | Founder-alpha testing exposed stale onboarding, squad, and Apple Health state leaking across accounts and blank transitions while bootstrap data was loading. A first-class bootstrap status plus immediate state reset keeps new-account onboarding and returning-account routing honest. | Max | Revisit if the app moves from the current session facade to a different state container or if backend bootstrap becomes incremental enough to support partial-shell loading. |
| 2026-03-19 | Move existing-user Apple Health management and destructive account actions into a dedicated Account hub. | Existing users need one reliable place to reconnect Apple Health, review saved summaries, sign out, transfer squad ownership, and delete an account without replaying onboarding or hunting inside the Squads surface. | Max | Revisit if Profile absorbs settings again or if health/integration management becomes large enough for its own dedicated route group. |
| 2026-03-19 | Treat physical-iPhone account deletion as a LAN-backed operation, and label failed Apple Health refreshes as historical rather than current. | Founder-alpha device testing exposed two reliability problems: the delete-account route can only work from a phone when the app points at the Mac-hosted backend over the LAN instead of `localhost`, and stale saved Apple Health summaries become misleading if they are rendered as live stats after a failed refresh. Making the backend path explicit and surfacing historical health summaries honestly keeps destructive actions functional and product trust intact during device testing. | Max | Revisit if delete-account moves fully onto Supabase Edge/serverless infra or if health sync gains background retries with fresher snapshot-state semantics. |
| 2026-03-19 | Treat onboarding completion as an explicit backend account flag, not as the presence of a synced profile row. | Every new auth user gets a blank `profiles` row immediately from the auth-sync trigger, which made fresh accounts look “ready” even though setup had never been completed. Keying founder-alpha routing off `auth_accounts.onboarding_completed` keeps new accounts in setup until the real onboarding bootstrap finishes. | Max | Revisit if auth bootstrap collapses into a different user-state model or if profiles stop being pre-created at signup. |
| 2026-03-19 | Replace the rolling last-24-hours Apple Health summary with a day-based summary plus a separate latest-sleep-session calculation. | Founder-alpha iPhone testing showed that day-facing metrics like steps and active energy were disagreeing with Apple Health because the app labeled a rolling 24-hour window as `today`, and sleep totals were inflated by overlapping stage samples. Using local-midnight-to-now for day metrics and a previous-day-noon sleep lookback with merged sleep sessions makes the visible summary match user expectations more closely. | Max | Revisit if the product adds richer per-source attribution, background sync, or a different sleep interpretation than “latest main sleep session.” |
| 2026-03-19 | Guard shared consistency recomputes during cascading user deletion, and cascade squad-only check-ins with their squad. | Founder-alpha delete-account testing found a real backend failure once a user had Apple Health snapshots and persisted check-ins: delete cascades were firing consistency triggers that tried to recreate summary rows for a user already disappearing from `auth.users`. The schema also allowed `check_ins.squad_id` to null out while `audience = 'squad'` still required a squad. The fix is to make shared consistency helpers no-op when the auth user is gone and to align squad-only post deletion with the squad FK contract. | Max | Revisit if delete-account moves away from Supabase auth-admin deletes or if squad-post retention semantics change. |
| 2026-03-19 | Make the Phase 2 mobile UX provider-aware while keeping Apple Health as the default proof source. | The product now needs to surface Apple Health, Strava, and WHOOP honestly in the mobile shell before every remote provider is fully live. Hydrating provider maps from Supabase, exposing provider management in Account, using explicit source selection plus Auto precedence in Check-in, and preserving lightweight provider provenance in Feed/Profile lets the app reflect the real integration roadmap without pretending the server-managed providers are already production-complete. | Max | Revisit once Strava and WHOOP are fully live and the team can decide whether the app still needs explicit source selection or can collapse back toward a simpler auto-only model. |
| 2026-03-19 | Use Google Stitch plus a repo-level `DESIGN.md` as the primary redesign workflow, while keeping Expo primitives and feature screens as the shipped source of truth. | Stitch supports App-mode generation, design systems, variants, prototypes, downloadable HTML/images, and theme extraction, which makes it a strong redesign accelerator. But Outtcast still needs stable React Native primitives, real mobile state handling, and product-specific behavior that should not come from pasted exported HTML. | Max | Revisit if Stitch outputs prove too inconsistent for multi-screen system work or if a manual design workflow clearly outpaces the Stitch loop. |
| 2026-03-18 | Use `Outtcast` as the visible in-app brand while keeping `Project Momentum` as the workspace codename, and keep prototype/demo language off shipped product surfaces. | The app needs to read like a real product during use and testing, while the repo can keep its internal operating context and development tools behind hidden debug access. | Max | Revisit if broader brand testing changes the final product name or if the repo/package identifiers are ready for a full rename. |
| 2026-03-18 | Keep demo-visible actions local-only, and keep `Only me` posts out of the Friends lane. | The bug-catcher audit showed that demo screens can look functional while still calling authenticated Supabase paths under the hood, and that private audience semantics break trust if `Only me` content is labeled or surfaced like a Friends post. Local demo fallbacks plus stricter audience handling keep the founder demo honest without changing the social thesis. | Max | Revisit when demo mode is retired or every surfaced action has a real backend-backed equivalent. |
| 2026-03-19 | Make Account the provider control center and Check-In the source-selection surface for Phase 2. | The founder-alpha product needs live provider state to feel trustworthy, but the right product surfaces are still private and task-oriented. Account is where connection health, sync state, and disconnect belong; Check-In is where source choice should shape the proof being posted. | Max | Revisit if provider management becomes large enough for a dedicated integrations area or if users need source selection in more places than Check-In. |
| 2026-03-19 | Treat Apple Health as the default workout proof source, with Strava and WHOOP as live server-managed providers that add optional source choice and recovery depth. | This preserves the v0.1 thesis that Apple Health remains required and default when usable, while still making remote providers materially valuable once connected. It also keeps token custody, refresh, and webhook logic off-device for Strava and WHOOP. | Max | Revisit if real provider overlap testing shows a different precedence model is more trustworthy, or if Apple Health stops being the right default anchor for the product. |
| 2026-03-19 | Patch `public.join_onboarding_squad(...)` directly onto the hosted Supabase project instead of waiting on migration-ledger cleanup. | The onboarding `Join Day ones` CTA was failing in the live app because the remote project was missing the checked-in `20260319090000_join_onboarding_squad.sql` function, even though the repo already had the correct RPC wrapper. Applying the SQL directly and triggering a PostgREST schema reload restored the user-facing flow immediately while preserving the need for later migration-history repair. | Max | Revisit when the hosted migration ledger is repaired and the direct remote patch can be reconciled back into normal migration operations. |
| 2026-03-19 | Use onboarding-first as the first serious Stitch redesign batch. | The onboarding flow plus the first check-in handoff is the smallest end-to-end path that proves whether Outtcast feels selective, trustworthy, and desirable before broader Home/Profile redesign work begins. | Max | Revisit if implementation shows the proof loop communicates the product better than onboarding, or if user testing says the first-session experience is no longer the highest-leverage surface. |
| 2026-03-19 | Choose the mineral-editorial `variant-a` Stitch direction as the current redesign winner, and implement `Cormorant Garamond + Manrope` directly in Expo. | `variant-a` is the closest match to the chosen visual thesis: high-end performance, light-performance base, soft luxury mineral contrast, mineral blue palette, fashion-editorial energy, and proof-first hierarchy. Shipping the real type system now removes design ambiguity and keeps the implementation pass aligned with the approved direction. | Max | Revisit if the real on-device implementation feels too soft, too sparse, or materially less usable than the exported Stitch direction. |
| 2026-03-20 | Harden the shared app shell so the Stitch onboarding translation cannot hang or drift on utility primitives. | The closer 1:1 pass needed a reliability layer underneath it: fail open on font errors, switch shared screens to safe-area-context, accept functional Pressable styles safely, and move badge/error states onto mineral-backed tokens so the new design system holds under real app conditions. | Max | Revisit if the shell primitives are replaced or the design system moves to a different theme/token structure. |
| 2026-03-20 | Translate the closer Stitch pass structurally, not just visually: onboarding should use editorial slabs and the first check-in should follow a four-stage ritual. | The first implementation wave proved the fonts and tokens, but the screens still read like a polished setup wizard and utility composer. Rebuilding the onboarding choices into larger slabs, making profile setup mission-led, collapsing Apple Health into fewer stronger surfaces, and restructuring check-in into `Move -> Room -> Proof -> Seal` keeps the shipped app much closer to the chosen Stitch direction without pasting exported HTML. | Max | Revisit if live user testing or device QA shows the sequence became too sparse, too slow, or harder to complete than the earlier utility layout. |
| 2026-03-20 | Push closer Stitch parity with abstract image-like editorial panels and more visible thin-line iconography before introducing real photo assets. | The remaining gap versus `variant-a` was graphic anchoring, not typography or tokens. Adding ambient mineral panels, monogram identity stamps, icon-backed selection slabs, health trust icon badges, and icon-enabled ritual controls moves the app much closer to Stitch while staying buildable in Expo and avoiding a premature image-asset pipeline. | Max | Revisit if later design passes need real photography crops, richer custom illustration, or if the abstract panels stop feeling premium on device. |
| 2026-03-22 | Use `EXPO_PUBLIC_BACKEND_URL=auto` as the preferred local dev-build setting for physical iPhone work. | The previous fixed-LAN-IP override broke whenever the Mac's address changed, which made device-side founder-alpha actions look flaky even when the backend and Metro were healthy. Treating `auto` as “follow the current Metro host on port 8787” keeps simulator and physical-iPhone dev builds aligned while preserving the option to pin a fixed LAN IP when needed. | Max | Revisit if release-style builds, tunnel workflows, or non-Metro device paths need a different backend resolution strategy. |
| 2026-03-22 | Patch the `fmt` pod to use a conservative C++ configuration under Xcode 26.4 device builds. | Once the iPhone 26.4 device-support path was repaired, physical-device builds moved forward and exposed a real native compile failure in the React Native `fmt` pod under Apple Clang's stricter C++20 consteval checks. Pinning the pod to `gnu++17` and disabling the `FMT_USE_CONSTEVAL` / `FMT_USE_NONTYPE_TEMPLATE_ARGS` paths in `ios/Podfile` keeps the local Expo/React Native build working on the newer Xcode while the upstream podspec catches up. | Max | Revisit after React Native / Expo updates the bundled `fmt` dependency or no longer requires the local post-install override. |
| 2026-03-23 | Treat untethered founder builds as Release or internal installs that disable the laptop-only backend by default. | The app's core founder-alpha flows now run directly against hosted Supabase, but standalone builds were still falling back to `localhost:8787` for privileged routes, which made delete-account and remote-provider actions fail opaquely off the Mac. Using `EXPO_PUBLIC_BACKEND_URL=disabled` for untethered builds keeps the core app usable on-device while surfacing honest limits until the privileged backend is hosted publicly. | Max | Revisit once the privileged backend has a public deployment or the team moves to a full TestFlight/App Store distribution path. |
| 2026-03-23 | Shim the EXConstants iOS build phase with a no-space `PROJECT_DIR` plus an explicit `PROJECT_ROOT` for Release device builds. | Untethered iPhone launches were still crashing inside `expo-linking` because Expo's `get-app-config-ios.sh` script reads `basename $PROJECT_DIR` without quoting it. In this repo's spaced path, that made the EXConstants phase exit before generating `EXConstants.bundle/app.config`, so Release builds had no embedded Expo manifest. Overriding the shell-phase environment in `ios/Podfile` keeps the bundled manifest generation working without moving the repo mid-sprint. | Max | Revisit if the repo moves to a no-space path or Expo fixes the script upstream. |
